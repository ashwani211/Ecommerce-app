import 'package:flutter/material.dart';

class CustomColors {
  // light theme colors
  static const backgroundColor = Colors.white;
  static const primaryColor = Colors.green;
  // static const appBarColor = Color(0xFF8081FF);

  // dark colors
  static const backgroundColorDark = Colors.black;
  static const appBarColorDark = Color(0xFFFF4081);

  // blue theme colors
  static const backgroundColorBlue = Colors.white;
  static const primaryColorBlue = Colors.blue;
}